#include "binary_trees.h"

/**
 * binary_tree_height - function that measures the height of a binary tree
 * @tree: points to the root node of the tree to measure thr height
 *
 * Return: nothing
 */

size_t binary_tree_height(const binary_tree_t *tree)
{
	if (tree == NULL)
		return (0);
	/* traverse right side and left side of tree, compare and return tne larger height */

}

/**
 * traverse - traverse through the tree
 * @tree: points to root of three
 * Return: height of tree
 */

size_t traverse(binary_tree_t *tree)
{
	size_t n;

	n = 0;
	if (tree)
	{	
		traverse(tree->left);
		retur;
	}


}



		

